# Content-Ranking-Engine
Built a content ranking engine on Microsoft's MSLR-WEB10K benchmark (1.2M query-document pairs, 136 features) comparing LightGBM LambdaMART and a CNN+LSTM neural ranker. Evaluated on NDCG@10, MAP, and MRR with paired t-test significance validation across 2,000 test queries.
